## Controlled Input + Live Preview

**Concept:** Controlled component (React state is the source of truth)

### Key points
- Input uses `value={state}` and `onChange`
- Every keystroke updates state → re-render
- Great for live validation and UI syncing
